import { useEffect, useState } from 'react'
import { Crown, Flame, Trophy } from 'lucide-react'
import { api } from '../lib/api'

export default function Leaderboard(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>('/users/leaderboard').then(setRows)},[]);return <div className="page"><div className="page-head"><div><span className="eyebrow small">NUBAGZ POINTS LEADERBOARD</span><h1>Participation, <em>ranked.</em></h1><p>Points rank meaningful NuBagz participation. My Trust remains a separate integrity signal and does not determine leaderboard position.</p></div></div><div className="podium">{rows.slice(0,3).map((r,i)=><div className={`podium-card p${i+1}`} key={r.username}>{i===0?<Crown/>:<Trophy/>}<span>#{i+1}</span><div className="avatar big">{r.username.slice(0,2).toUpperCase()}</div><strong>{r.username}</strong><b>{Number(r.bag_score||0).toLocaleString()} Points</b><small><Flame/> {r.streak_days} day streak</small></div>)}</div><section className="panel leaderboard-table"><div className="leader-head phase1-points-grid"><span>RANK</span><span>USER</span><span>POINTS</span><span>STREAK</span></div>{rows.map(r=><div className="leader-row phase1-points-grid" key={r.rank}><b>#{r.rank}</b><div><div className="avatar small">{r.username.slice(0,2).toUpperCase()}</div><strong>{r.username}</strong></div><span>{Number(r.bag_score||0).toLocaleString()}</span><span><Flame/> {r.streak_days}</span></div>)}</section></div>}
