import { useEffect, useState } from 'react'
import { Crown, Flame, Trophy } from 'lucide-react'
import { api } from '../lib/api'

export default function Leaderboard(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>('/users/leaderboard').then(setRows)},[]);return <div className="page"><div className="page-head"><div><span className="eyebrow small">THE BAGBOARD</span><h1>Reputation beats <em>capital.</em></h1><p>BagScore rewards genuine participation — not how much money you deposited.</p></div></div><div className="podium">{rows.slice(0,3).map((r,i)=><div className={`podium-card p${i+1}`} key={r.username}>{i===0?<Crown/>:<Trophy/>}<span>#{i+1}</span><div className="avatar big">{r.username.slice(0,2).toUpperCase()}</div><strong>{r.username}</strong><b>{r.bag_score} BagScore</b><small><Flame/> {r.streak_days} day streak</small></div>)}</div><section className="panel leaderboard-table"><div className="leader-head"><span>RANK</span><span>BAGGER</span><span>BAGSCORE</span><span>XP</span><span>STREAK</span></div>{rows.map(r=><div className="leader-row" key={r.rank}><b>#{r.rank}</b><div><div className="avatar small">{r.username.slice(0,2).toUpperCase()}</div><strong>{r.username}</strong></div><span>{r.bag_score}</span><span>{r.xp.toLocaleString()}</span><span><Flame/> {r.streak_days}</span></div>)}</section></div>}
