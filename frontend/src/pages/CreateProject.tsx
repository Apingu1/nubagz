import { useState } from 'react'
import { ArrowLeft, ArrowRight, Rocket, ShieldCheck } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { api } from '../lib/api'

export default function CreateProject(){const nav=useNavigate();const [f,setF]=useState({name:'',symbol:'',description:'',website:'',chain:'Avalanche',treasury_address:''});const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');try{await api('/projects',{method:'POST',body:JSON.stringify({...f,symbol:f.symbol.toUpperCase()})});nav('/app/studio')}catch(e:any){setError(e.message)}}
 return <div className="page narrow"><Link to="/app/studio" className="back-link"><ArrowLeft/> Creator Studio</Link><div className="form-hero"><Rocket/><span>NEW PROJECT</span><h1>Bring your project into <em>NuBagz.</em></h1><p>Listing is free. Approval protects earners from obvious scams, malicious links and fake projects.</p></div><form className="builder panel" onSubmit={submit}><div className="form-grid"><label>Project name<input required value={f.name} onChange={e=>setF({...f,name:e.target.value})} placeholder="Neon Frog"/></label><label>Token symbol<input required value={f.symbol} onChange={e=>setF({...f,symbol:e.target.value})} placeholder="NFROG"/></label><label>Network<select value={f.chain} onChange={e=>setF({...f,chain:e.target.value})}><option>Avalanche</option><option>Solana</option><option>Ethereum</option><option>Base</option><option>Arbitrum</option><option>Other</option></select></label><label>Website<input value={f.website} onChange={e=>setF({...f,website:e.target.value})} placeholder="https://…"/></label><label className="full-span">Project description<textarea required minLength={20} value={f.description} onChange={e=>setF({...f,description:e.target.value})} placeholder="Explain what the project is and what users can do with it…"/></label><label className="full-span">Treasury / distribution address <small>optional for initial review</small><input value={f.treasury_address} onChange={e=>setF({...f,treasury_address:e.target.value})} placeholder="Project-controlled reward treasury"/></label></div><div className="builder-note"><ShieldCheck/><p>Creating a project does not list a token for sale and NuBagz does not raise capital on your behalf. This profile is used to create participation campaigns after approval.</p></div>{error&&<div className="form-error">{error}</div>}<button className="btn primary big">Submit project <ArrowRight/></button></form></div>
}
