import { useState } from 'react'
import { ArrowLeft, ArrowRight, Rocket, ShieldCheck } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { api } from '../lib/api'
import type { Project } from '../types'

const chains=['Robinhood','Avalanche','Ethereum','Base','Arbitrum','Polygon']

export default function CreateProject(){
 const nav=useNavigate();const [saving,setSaving]=useState(false);const [error,setError]=useState('');const [f,setF]=useState({name:'',symbol:'',description:'',website:'',chain:'Robinhood',logo_url:'',treasury_address:''})
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setSaving(true);try{const project=await api<Project>('/projects',{method:'POST',body:JSON.stringify({...f,symbol:f.symbol.toUpperCase(),website:f.website.trim()||null,logo_url:f.logo_url.trim()||null,treasury_address:f.treasury_address.trim()||null})});nav(`/app/studio/challenges/new?project=${project.id}&projectCreated=1`)}catch(e:any){setError(e.message||'Could not create project')}finally{setSaving(false)}}
 return <div className="page narrow"><Link to="/app/studio" className="back-link"><ArrowLeft/> Creator Studio</Link><div className="form-hero"><Rocket/><span>NEW PROJECT</span><h1>Create the <em>project first.</em></h1><p>NuBagz V2 keeps the domain simple: create a Project, maintain its Trust profile, then add independent Challenges underneath it.</p></div>{error&&<div className="form-note">{error}</div>}<form className="builder panel" onSubmit={submit}><h3>01 — Project identity</h3><div className="form-grid"><label>Project name<input required minLength={2} value={f.name} onChange={e=>setF({...f,name:e.target.value})} placeholder="Project name"/></label><label>Token / symbol<input required value={f.symbol} onChange={e=>setF({...f,symbol:e.target.value.toUpperCase()})} placeholder="TOKEN"/></label><label>Chain<select value={f.chain} onChange={e=>setF({...f,chain:e.target.value})}>{chains.map(x=><option key={x}>{x}</option>)}</select></label><label>Website <small>optional</small><input value={f.website} onChange={e=>setF({...f,website:e.target.value})} placeholder="https://…"/></label><label className="full-span">Description<textarea required minLength={20} value={f.description} onChange={e=>setF({...f,description:e.target.value})} placeholder="Describe what the project is, what users should understand, and what it is building."/></label><label>Logo URL <small>optional</small><input value={f.logo_url} onChange={e=>setF({...f,logo_url:e.target.value})} placeholder="https://…"/></label><label>Treasury address <small>optional</small><input value={f.treasury_address} onChange={e=>setF({...f,treasury_address:e.target.value})} placeholder="0x…"/></label></div><div className="builder-note"><ShieldCheck/><p>Creating a Project is not a NuBagz endorsement. Public integrity signals belong in Project Trust and can be maintained from Creator Studio after creation.</p></div><button className="btn primary" disabled={saving}>{saving?'Creating…':<>Create project & add Challenge <ArrowRight/></>}</button></form></div>
}
